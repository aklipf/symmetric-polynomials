# Ideas

## Compilation

It is (probably) possible to compile for a fixed domain size by knowing all existing invarient and a single solution. Then, we can check wich invarients will be not zero given the solution to compute the coefficients of the symmetric polynomial that give the model count.
